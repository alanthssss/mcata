/**
 * Progression theme — re-export of the default commercial shell.
 * Kept as a named export so the theme registry can reference it
 * by a stable identifier.
 */
export { defaultTheme as progressionTheme } from './defaultTheme';
